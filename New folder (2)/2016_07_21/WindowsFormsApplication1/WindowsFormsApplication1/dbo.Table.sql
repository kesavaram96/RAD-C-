CREATE TABLE [dbo].[Table] (
    [StudentID] INT        NOT NULL,
    [Name]      NCHAR (10) NULL,
    [DepID]     INT        NULL,
    [Mark1]     INT        NULL,
    [Mark2]     INT        NULL,
    PRIMARY KEY CLUSTERED ([StudentID] ASC)
);

